Add this to your `bash_profile.sh` so you can just call it when you're in this directory:
`coffeasing='./shell coffeateam/coffea-dask-almalinux9:2025.10.2-py3.12'`

You might have to update the docker image, but I haven't need to in a while.
